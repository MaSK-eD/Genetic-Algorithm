for i=1:1:30
    a=mod(i,10);
    if a>=1 && a<=6
        display(i)
    end
end
for i=990:1:1000
    a=mod(i,10);
    if a>=1 && a<=6
        display(i)
    end
end
for i=1000:1:2000
    a=mod(i,10);
    if a>=7 && a<=9 || a==0
        display(i)
    end
end

    